import React from "react";
import Wallet from "./Wallet";

const headerStyle = {
  backgroundColor: "#001529",
  color: "#fff"
};

export default () => (
  <header style={headerStyle}>
    <Wallet />
  </header>
);
